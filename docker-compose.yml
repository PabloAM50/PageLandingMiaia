version: '3'

services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always
    # Si necesitas variables de entorno, descomenta y añade las que necesites
    # environment:
    #   - NODE_ENV=production
